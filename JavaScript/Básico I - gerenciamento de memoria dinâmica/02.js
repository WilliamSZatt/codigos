const nota1 = 10;
const nota2 = 8;

const somaAluno = nota1 + nota2;
let mediaNotas = somaAluno / 2; // Pode fazer  1 por 1

mediaNotas = (nota1 + nota2) / 2; // Pode fazer direto

console.log("Soma das notas =", somaAluno);
console.log("Media das notas =", mediaNotas);

const numero = 16;

console.log("Raiz quadrada de 16 =", Math.sqrt(numero)); // calculanado raiz quadrada

// Math = Biblioteca de Matemética
// .sqrt = função para calcular a raiz que acompanha a biblioteca matemática