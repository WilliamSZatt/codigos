// Operador Ternário

const idade = 21;

// ? (Interrogação) = Ele estaria perguntando (como um if)
// Depois do ? seria a resposta se desse verdadeiro
// Os dois pontos (:) são a divisão entre o verdadeiro e falso (Como um else)
// Depois dos dois pontos (:) seria a resposta se desse falso

// idade >= 18 ? console.log("Maior de idade") : console.log("Menor de idade");

const mensagem = idade >= 18 ? "Maior de idade" : "Menor de idade"; //! Esse codigo se chama INLINE (Na Mesma linha)

console.log(mensagem); // Retornando o valor da variável mensagem

// condições falsas = 0, undefinrd, null, string vazia e NaN

// condições Verdadeiras = tudo que não está nas condições falsas
